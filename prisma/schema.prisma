generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// ─── NextAuth ───────────────────────────────────────────

model User {
  id            String    @id @default(cuid())
  name          String?
  email         String?   @unique
  emailVerified DateTime?
  image         String?
  role          Role      @default(USER)
  createdAt     DateTime  @default(now())

  accounts  Account[]
  sessions  Session[]
  practiceLists PracticeList[]
  progress  UserProgress[]
}

model Account {
  id                String  @id @default(cuid())
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String? @db.Text
  access_token      String? @db.Text
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String? @db.Text
  session_state     String?

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
}

model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique
  userId       String
  expires      DateTime

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
}

// ─── Domínio ────────────────────────────────────────────

model Area {
  id          String   @id @default(cuid())
  slug        String   @unique
  name        String
  description String
  icon        String?
  order       Int      @default(0)
  published   Boolean  @default(true)

  topics      Topic[]          // ← relação com Topic
}

model Topic {
  id          String   @id @default(cuid())
  areaId      String
  slug        String   @unique
  name        String
  order       Int      @default(0)

  area        Area         @relation(fields: [areaId], references: [id])
  practiceLists PracticeList[]
  simulations Simulation[]
}

model Simulation {
  id          String  @id @default(cuid())
  topicId     String
  slug        String  @unique
  name        String
  description String
  parameters  Json
  order       Int     @default(0)
  published   Boolean @default(true)

  topic    Topic          @relation(fields: [topicId], references: [id])
  progress UserProgress[]
}

model UserProgress {
  id           String   @id @default(cuid())
  userId       String
  simulationId String
  lastParams   Json
  timesOpened  Int      @default(1)
  updatedAt    DateTime @updatedAt

  user       User       @relation(fields: [userId], references: [id], onDelete: Cascade)
  simulation Simulation @relation(fields: [simulationId], references: [id])

  @@unique([userId, simulationId])
}

model PracticeList {
  id          String   @id @default(cuid())
  userId      String
  topicId     String
  exercicios  Json     // array de exercícios gerados
  score       Int?     // pontuação se completou
  createdAt   DateTime @default(now())

  user  User  @relation(fields: [userId],  references: [id], onDelete: Cascade)
  topic Topic @relation(fields: [topicId], references: [id])
}

// ─── Enums ──────────────────────────────────────────────

enum Role {
  USER
  ADMIN
}